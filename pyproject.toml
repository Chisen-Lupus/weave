[build-system]
requires = ['setuptools>=61']
build-backend = 'setuptools.build_meta'

[project]
name = 'weave'
version = '0.1.0'
description = 'WEAVE'
requires-python = '>=3.10'

[tool.setuptools]
packages = ['weave']